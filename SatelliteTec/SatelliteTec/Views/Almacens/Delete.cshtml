@model SatelliteTec.Models.Almacen

@{
    ViewBag.Title = "Eliminar Almacen";
}






<div style="margin-top: 50px">
    <div class="shadow p-3 mb-5 bg-white rounded">

        <h2 style="text-align: center">@ViewBag.Title</h2>

    </div>


    <div class="card">
        <h5 class="card-header">

            <label style="cursor: pointer;" onclick="location.href='@Url.Action("Index", "Almacens")'"><i class="far fa-hand-point-left fa-lg"></i> Regresar a la Lista</label>
        </h5>
        <div class="card-body">
            <h5 class="card-title">¿Estás segura de que quieres eliminar esto?</h5>


            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Estado)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Estado)
                </dd>
            </dl>


            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <button type="submit" class="btn btn-danger"><i class="fas fa-trash fa-sm"></i> Eliminar</button>
            }


        </div>
    </div>

</div>